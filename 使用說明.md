# 使用說明

本文件將指導您如何設定並啟動本專案的開發環境。

---

## 專案結構

- **`/` (根目錄)**: 包含後端 FastAPI 應用程式及相關設定。
- **`/frontend`**: 包含前端 Vue.js 應用程式。

---

## 後端 (FastAPI)

### 1. 安裝依賴

請確認您已安裝 Python。進入專案根目錄，建議先建立並啟用虛擬環境。

```bash
# 進入專案根目錄
cd d:\Python\GFmoter

# 安裝所有必要的 Python 套件
pip install -r requirements.txt
```

### 2. 環境變數

複製 `.env.sample` 檔案並重新命名為 `.env`。

```bash
copy .env.sample .env
```

請根據您的本地端資料庫設定，修改 `.env` 檔案中的 `DATABASE_URL`。

### 3. 啟動伺服器

啟動 API 前，請先套用資料庫 migration：

```bash
alembic upgrade head
```

在專案根目錄下執行以下指令：

```bash
uvicorn main:app --reload
```

伺服器預設會啟動於 `http://127.0.0.1:8000`。

### 4. 資料庫 Migration

本專案使用 Alembic 管理資料庫 schema。修改 `db/models.py` 後，請建立新的 migration 並檢查內容：

```bash
alembic revision --autogenerate -m "描述本次資料庫變更"
alembic upgrade head
alembic current
alembic heads
```

正式部署前請先備份資料庫，並優先在 staging 或本機資料庫副本執行 `alembic upgrade head`。

### 5. 管理員忘記密碼

一般管理員忘記密碼時，請由「最高級」管理員登入後台「系統與權限」頁面，編輯該管理員並設定新密碼。

若最高級管理員也無法登入，請先備份資料庫，再於可信任的伺服器或本機環境執行緊急重設指令。此指令只會替換密碼 hash，不會顯示或還原舊密碼。

```bash
python scripts/reset_admin_password.py 管理員帳號
```

若需要在無互動環境執行，可傳入新密碼；正式環境請注意 shell history 風險。

```bash
python scripts/reset_admin_password.py 管理員帳號 --password "新密碼"
```

如果資料庫沒有任何可登入的最高級管理員，可明確建立一個緊急帳號：

```bash
python scripts/reset_admin_password.py admin --create-if-missing --role "最高級"
```

完成後請立刻登入後台，改成正式密碼，並移除不再需要的緊急帳號。

---

## 前端 (Vue.js)

### 1. 安裝依賴

請確認您已安裝 Node.js 與 npm。進入前端專案目錄。

```bash
# 進入前端專案目錄
cd frontend
# 或者
cd d:\Python\GFmoter\frontend

# 安裝所有必要的 Node.js 套件
npm install
```

### 2. 啟動開發伺服器

在 `frontend` 目錄下執行以下指令：

```bash
npm run dev
```

前端開發伺服器預設會啟動於 `http://localhost:5173`。您可以開啟瀏覽器進入此網址來查看網站。
