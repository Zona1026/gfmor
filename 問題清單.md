# 問題清單

- **問題**: 在 Windows PowerShell 環境下初始化前端專案時，執行 shell 指令遇到問題。
  - `&&` 語彙基元不是有效的陳述式分隔符號，無法串接多個指令。
  - `mkdir` 指令一次無法建立多個目錄，且在目錄已存在時會報錯。
- **狀態**: 已修正 (改為分次執行指令，並使用 `-ErrorAction SilentlyContinue` 忽略錯誤)